DATABASE_URL=mysql://root:rasikh@localhost:3306/react_hono_starter
DATABASE_HOST=localhost
DATABASE_USER=root
DATABASE_PASSWORD=rasikh
DATABASE_NAME=react_hono_starter
DATABASE_PORT=3306

JWT_SECRET=xEDdedSoN+pC6iGsiDKQ0xStyd2mUAEyGLbxlHQ4V2I=
ENV=local

# Google OAuth (GIS popup auth-code flow — origins must match Google Console redirect URIs exactly)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
# SPA origin only for GIS popup auth-code — must match Authorized redirect URIs in Google Console (restart server after changing .env).
GOOGLE_REDIRECT_URI=http://localhost:3000

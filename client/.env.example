VITE_BACKEND_URL=http://localhost:4000

# Same OAuth Web client ID as GOOGLE_CLIENT_ID on the server (public in the browser).
# Optional: omit to hide Google sign-in until configured.
VITE_GOOGLE_CLIENT_ID=
